@model IEnumerable<ShopThoiTrangNam.Models.ShoppingCart>

@{
    ViewData["Title"] = "Giỏ hàng của bạn";
    decimal totalAmount = Model.Sum(i => i.Price * i.Quantity); 
}

<div class="container my-5">
    <h2 class="fw-bold mb-4 text-center">🛒 Giỏ hàng của bạn</h2>

    @if (!Model.Any())
    {
        <div class="alert alert-info text-center">
            Giỏ hàng của bạn hiện đang trống. 	
            <a asp-controller="Store" asp-action="Index" class="alert-link">Tiếp tục mua sắm</a>.
        </div>
    }
    else
    {
        <table id="cart-table" class="table table-bordered align-middle shadow-sm">
            <thead class="table-dark text-center">
                <tr>
                    <th>Ảnh</th>
                    <th>Sản phẩm</th>
                    <th>Màu</th>
                    <th>Size</th>
                    <th>Giá</th>
                    <th>Số lượng</th>
                    <th>Tổng</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    var product = item.Product;
                    // Lấy giá dưới dạng số cho JS tính toán
                    <tr class="cart-row" data-cart-id="@item.CartId" data-price="@item.Price.ToString(System.Globalization.CultureInfo.InvariantCulture)">
                        <td style="width:120px; text-align:center;">
                            <img src="@product?.ImageUrl" alt="@product?.ProductName" class="img-thumbnail" style="width:100px; height:100px; object-fit:cover;" />
                        </td>
                        <td>
                            <strong>@product?.ProductName</strong><br />
                            <small class="text-muted">@product?.Category?.CategoryName</small>
                        </td>
                        <td class="text-center">@item.Color</td>
                        <td class="text-center">@item.Size</td>
                        <td class="text-end text-danger item-price">@item.Price.ToString("N0") ₫</td>
                        <td class="text-center" style="width:140px;">
                            <input type="number" 
                                            name="quantity" 
                                            value="@item.Quantity" 
                                            min="1" 
                                            class="form-control text-center quantity-input" 
                                            data-cart-id="@item.CartId"
                                            style="width:80px; display:inline-block;" />
                        </td>
                        <td class="text-end fw-bold text-primary item-total">@((item.Price * item.Quantity).ToString("N0")) ₫</td>
                        <td class="text-center">
                            <form asp-action="Remove" method="post" onsubmit="return confirm('Bạn có chắc muốn xóa sản phẩm này khỏi giỏ hàng?');">
                                <input type="hidden" name="id" value="@item.CartId" />
                                <button type="submit" class="btn btn-outline-danger btn-sm">Xóa</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="d-flex justify-content-between align-items-center mt-4">
            <a asp-controller="Store" asp-action="Index" class="btn btn-outline-secondary">← Tiếp tục mua sắm</a>

            <div class="text-end">
                <h5 class="fw-bold">
                    Tổng cộng: 
                    <span class="text-danger" id="grand-total">@totalAmount.ToString("N0") ₫</span>
                </h5>
                <a href="#" class="btn btn-success px-4 mt-2">Thanh toán</a>
            </div>
        </div>
    }
</div>

@section Scripts {
<script>
    document.addEventListener('DOMContentLoaded', function () {
        // Hàm định dạng số tiền (Đảm bảo định dạng tiền tệ Việt Nam)
        function formatCurrency(number) {
            return number.toLocaleString('vi-VN');
        }

        // Hàm cập nhật tổng tiền cho một hàng và Tổng cộng chung
        function updateTotals(cartId, newQuantity) {
            const row = document.querySelector(`.cart-row[data-cart-id="${cartId}"]`);
            if (!row) return;

            // Đảm bảo dấu thập phân là dấu chấm để parseFloat hoạt động chính xác
            const price = parseFloat(row.dataset.price.replace(/,/g, '.'));
            
            if (isNaN(price)) return;

            const itemTotal = price * newQuantity;

            // 1. Cập nhật Tổng tiền của hàng
            row.querySelector('.item-total').textContent = formatCurrency(itemTotal) + ' ₫';

            // 2. Cập nhật Tổng cộng chung
            let grandTotal = 0;
            document.querySelectorAll('.cart-row').forEach(function(cartRow) {
                const rowPrice = parseFloat(cartRow.dataset.price.replace(/,/g, '.'));
                const rowQuantity = parseInt(cartRow.querySelector('.quantity-input').value);
                if (!isNaN(rowPrice) && !isNaN(rowQuantity)) {
                    grandTotal += rowPrice * rowQuantity;
                }
            });

            document.getElementById('grand-total').textContent = formatCurrency(grandTotal) + ' ₫';
        }

        // Hàm gọi Fetch API để cập nhật số lượng trong DB
        async function updateQuantityInDb(cartId, quantity) {
            const url = '@Url.Action("UpdateQuantity", "ShoppingCart")';

            // Sửa lỗi: Sử dụng URLSearchParams để gửi dữ liệu dưới dạng form data
            const formData = new URLSearchParams();
            formData.append('id', cartId);
            formData.append('quantity', quantity);

            try {
                // Controller mong đợi dữ liệu dưới dạng Form Data/Query String, không phải JSON.
                const response = await fetch(url, {
                    method: 'POST',
                    // Bỏ 'Content-Type': 'application/json' để gửi form data
                    body: formData 
                });

                // Xử lý lỗi HTTP (4xx, 5xx)
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                const data = await response.json();

                if (data.success) {
                    console.log("Cập nhật giỏ hàng thành công!");
                } else {
                    // Xử lý lỗi logic (ví dụ: số lượng vượt quá tồn kho)
                    alert(data.message || "Không thể cập nhật giỏ hàng.");
                    // Tải lại trang để đồng bộ
                    location.reload(); 
                }
            } catch (error) {
                console.error("Lỗi server khi cập nhật giỏ hàng:", error);
                alert("Lỗi server khi cập nhật giỏ hàng.");
                location.reload(); 
            }
        }

        // ----------------------------------------------------
        // Xử lý sự kiện khi số lượng thay đổi (Fetch API)
        // ----------------------------------------------------
        let timeout = null;
        
        document.querySelectorAll('.quantity-input').forEach(inputElement => {
            inputElement.addEventListener('input', function () {
                const cartId = this.dataset.cartId;
                let newQuantity = parseInt(this.value);

                if (newQuantity < 1 || isNaN(newQuantity)) {
                    newQuantity = 1;
                    this.value = 1;
                }

                // Cập nhật tổng tiền tức thì trên giao diện
                updateTotals(cartId, newQuantity);

                // Xóa timeout cũ nếu có (debounce)
                if (timeout) {
                    clearTimeout(timeout);
                }

                // Đặt timeout mới để gọi Fetch API sau 500ms
                timeout = setTimeout(function () {
                    // Gọi hàm Fetch API
                    updateQuantityInDb(cartId, newQuantity);
                }, 500);
            });
        });
    });
</script>
}